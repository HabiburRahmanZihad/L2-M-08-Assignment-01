# ✅ **Problem Solving with TypeScript**

````md

# 📝 Blog Post (Bangla)

## ১. TypeScript এ `interface` এবং `type` এর মধ্যে পার্থক্য

TypeScript এ `interface` এবং `type`—দুটোই object-এর shape বা কাঠামো নির্ধারণ করতে ব্যবহৃত হয়। তবে এদের মাঝে কিছু গুরুত্বপূর্ণ পার্থক্য রয়েছে।

### Interface
`interface` মূলত object, class এবং structure সংজ্ঞায়ন করতে ব্যবহৃত হয়। ভবিষ্যতে এটিকে **extend** বা **implement** করা সহজ।

```ts
interface User {
  name: string;
  age: number;
}
````

### Type

`type` আরও flexible। এটি শুধুমাত্র object নয় বরং union, intersection, tuple এবং primitive alias হিসেবেও কাজ করতে পারে।

```ts
type ID = string | number;
```

### পার্থক্য সংক্ষেপে:

| বিষয়           | interface              | type                          |
| -------------- | ---------------------- | ----------------------------- |
| Extending      | সহজ                    | করা যায়, তবে সিনট্যাক্স ভিন্ন |
| Union support  | নেই                    | আছে                           |
| Merge করা      | সম্ভব                  | সম্ভব নয়                      |
| প্রধান ব্যবহার | object/class structure | flexible type design          |

**উপসংহার:**
Object/class structure এর জন্য `interface`, আর flexible টাইপ ডিফাইন করার জন্য `type` বেশি উপযোগী।

---

## ২. TypeScript এ `any`, `unknown`, এবং `never` এর পার্থক্য

### any — সবচেয়ে শিথিল টাইপ

`any` টাইপ চেকিং বন্ধ করে দেয়। যেকোনো ভ্যালু এতে রাখা যায় এবং ব্যবহার করাও যায়।

```ts
let value: any = 10;
value = 'Hello';
value = true;
```

➡️ এটি অসতর্ক ব্যবহারে বাগ তৈরি করতে পারে।

---

### unknown — নিরাপদ any

`unknown` এ যেকোনো ভ্যালু রাখা যায়, কিন্তু ব্যবহার করার আগে অবশ্যই টাইপ চেক করতে হয়।

```ts
let data: unknown = 'World';

if (typeof data === 'string') {
  console.log(data.toUpperCase());
}
```

➡️ এটি any-এর তুলনায় অনেক বেশি নিরাপদ।

---

### never — কখনো ঘটে না এমন পরিস্থিতির টাইপ

যেখানে কোন function কখনো return করে না, যেমন error throw করা বা infinite loop।

```ts
function throwError(msg: string): never {
  throw new Error(msg);
}
```

➡️ এটি সবচেয়ে strict টাইপ।

---

### পার্থক্য সংক্ষেপে:

| টাইপ        | সংক্ষেপে ব্যাখ্যা                              |
| ----------- | ---------------------------------------------- |
| **any**     | যেকোনো ভ্যালু; টাইপ চেক নেই                    |
| **unknown** | যেকোনো ভ্যালু; ব্যবহার করার আগে টাইপ চেক দরকার |
| **never**   | কোন ভ্যালু থাকে না; function return করে না     |

---

# 📎 Submission Details

* All solutions are inside **solution.ts**
* Blog post included in this README.md
* Function names and output formatting strictly follow the assignment instructions
* No console.log inside the submitted solution file (except required inside `printBookDetails`)

---